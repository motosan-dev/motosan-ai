#!/usr/bin/env bash
if ! has nix_direnv_version || ! nix_direnv_version 3.0.7; then
    source_url "https://raw.githubusercontent.com/nix-community/nix-direnv/3.0.7/direnvrc"
fi

use flake

watch_file ./flake.nix
watch_file ./devshell/default.nix
