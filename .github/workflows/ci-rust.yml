name: Rust CI

on:
  pull_request:
    paths:
      - "sdks/rust/**"
      - ".github/workflows/ci-rust.yml"
  push:
    branches: [main]
    paths:
      - "sdks/rust/**"
      - ".github/workflows/ci-rust.yml"

jobs:
  rust:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sdks/rust
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Cargo fmt
        run: cargo fmt --all -- --check
      - name: Cargo clippy
        run: cargo clippy --all-features --all-targets -- -D warnings
      - name: Cargo test
        run: cargo test --all-features
      - name: Install cargo-hack
        uses: taiki-e/install-action@cargo-hack
      - name: Cargo hack (each feature)
        run: cargo hack check --each-feature --no-dev-deps

  rust-msrv-no-features:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sdks/rust
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.82.0
      - name: Pin transitive deps for MSRV
        # indexmap >=2.14 requires hashbrown >=0.17, which itself requires
        # the unstable `edition2024` Cargo feature — unavailable on 1.82.
        # uuid >=1.23 pulls getrandom 0.4, which also requires edition2024.
        # The codex-oauth live-smoke dev graph otherwise resolves newer
        # reqwest/url/TLS/WASI transitives whose MSRVs are newer than 1.82.
        # Cargo.lock is gitignored (library crate), so without these pins
        # CI resolves transitively to the latest compatible-looking versions
        # on every run.
        run: |
          cargo generate-lockfile
          cargo update -p indexmap --precise 2.13.1
          cargo update -p uuid --precise 1.18.1
          cargo update -p reqwest --precise 0.12.4
          cargo update -p url --precise 2.4.1
          cargo update -p native-tls --precise 0.2.15
          cargo update -p tempfile --precise 3.24.0
          cargo update -p zeroize --precise 1.8.2
          cargo update -p wasip2 --precise 1.0.1+wasi-0.2.4
      - name: Cargo build (no features)
        run: cargo build
      - name: Cargo test (no features)
        run: cargo test
